#include<stdio.h>
#include<stdint.h>
void main()
{
        uint32_t x,a[x],i;
        scanf("%d",&x);
        for(i=0;i<x;i++)
        {
            a[i]=1;
            printf("%d",a[i]);
        }

}
